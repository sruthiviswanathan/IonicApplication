// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: "AIzaSyCpwrF1jvhTPa66i7EerVGdKL_8fqrPxDs",
    authDomain: "pinch-of-yum.firebaseapp.com",
    databaseURL: "https://pinch-of-yum.firebaseio.com",
    projectId: "pinch-of-yum",
    storageBucket: "pinch-of-yum.appspot.com",
    messagingSenderId: "170148146433",
    appId: "1:170148146433:web:99a663fb331ba2e8173f00",
    measurementId: "G-66XRYX7JYD"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
